-	in lines with macroname for expansion, check before the line: (expand_macros_handle_command_state)
		strstr lookup of each declared macroname against words in line
		if exists, assuming lines started with non blank, check for a blank before that index - if exists, return an error
-	labels should conform to digit and alphabetic characters only, no blanks between label and ':' 
-	add registers as reserved words
-	are_type for command ?
-	show multiple errors per line? 
-	complete handle addressing
-	complete no arguments section in command read save arguments
-	dive deeper into errors possible when reading words for commands
